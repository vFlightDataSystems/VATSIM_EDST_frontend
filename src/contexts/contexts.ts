import {createContext} from "react";

export const EdstContext = createContext(null);
export const AclContext = createContext(null);
export const DepContext = createContext(null);
export const TooltipContext = createContext(null);