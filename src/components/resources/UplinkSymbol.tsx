import React from "react";
import { EdstButton } from "./EdstButton";
import { UPLINK_SYMBOL } from "../../constants";

export const UplinkSymbol = () => {
  return <EdstButton margin="0 4px">{UPLINK_SYMBOL}</EdstButton>;
};
